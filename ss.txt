Berikut laporan lengkap hasil observasi terhadap dokumentasi, infrastruktur, dan implementasi testing platform:
Ringkasan Eksekutif
Dari ~14 package/app, hanya 3 yang memiliki test (api, shared, validation). Coverage kode < 20% overall. Banyak area yang memiliki dokumentasi testing lengkap tapi nol implementasi.
1. Kesenjangan Dokumentasi vs Implementasi
Dokumen	Isi	Realita
docs/testing/testing-strategy.md	Target coverage 80%, Stack: Vitest + Testing Library + Playwright + Supertest	Coverage ~15-20%, Testing Library tidak terinstal, Playwright tidak ada, Supertest di package.json tapi tidak dipakai
docs/testing/e2e-flows.md	30 E2E flow (P0-P2), Playwright, multi-browser	Nol implementasi — tidak ada file Playwright satupun
docs/testing/test-cases.md (Part 1 & 2)	~150+ test case skenario (AUTH, BOOK, CUST, ADMIN, dll)	Banyak skenario belum dicover oleh unit test (lihat detail di bawah)
manual-test.md	128 manual test case, 19 area	Nol automasi — semua manual, belum ada satupun tercentang
2. File/Layer Tanpa Test Sama Sekali
apps/api/src/lib/ — 9 dari 11 file tanpa test (18% coverage)
File	Fungsi
auth.ts	JWT sign/verify, Argon2 hash/verify, refresh token
audit.ts	Audit log creation
cms.ts	Directus sync / cache
db.ts	Drizzle client init
email.ts	Nodemailer transport
email-templates.ts	Template HTML email
notification.ts	In-app + WhatsApp notification
redis.ts	Redis client & caching
storage.ts	File upload (local/R2)
apps/api/src/middleware/ — 3 dari 3 file tanpa test (0% coverage)
File	Fungsi
auth.ts	Auth middleware + RBAC requireRole
error-handler.ts	Global error handler
rate-limiter.ts	Rate limiting
packages/database/ — 36 file tanpa test (0% coverage)
- 32 table schemas, relations, client, seeds — tidak ada satu test pun
packages/ui/ — 13 komponen tanpa test (0% coverage)
- Button, Modal, Input, Table, DatePicker, dll — padahal testing-strategy.md mewajibkan component test
apps/web/ — ~30 halaman tanpa test (0% coverage)
- Tidak ada Vitest, tidak ada Playwright, tidak ada test framework sama sekali
3. Kesenjangan Kualitas pada Test yang Ada
Masalah Sistematis (semua file test API)
1. Response body tidak pernah diperiksa — Semua test cuma cek res.status, tidak ada yang baca res.json() untuk verifikasi data, pesan error, token dll
2. Mock tidak diverifikasi — createAuditLog, createNotification, sendEmail dll dipanggil di production code tapi test tidak pernah ngecek toHaveBeenCalled()
3. Rate limiter selalu di-skip — Mock rate limiter no-op, enforcement tidak pernah diuji
4. Edge case validation tidak ada — Boundaries, null/undefined, format invalid jarang diuji
Kesenjangan Per Endpoint (yang sudah ada test)
Route File	Endpoints di Source	Covered	Missing
bookings.test.ts	~15 endpoints	~11	GET /:id, POST /:id/reject, POST /:id/on-the-way, POST /:id/start, POST /:id/complete
auth.test.ts	~11 endpoints	~8	POST /convert-guest, POST /verify-email, POST /resend-verification
payments.test.ts	~4 endpoints	~3	Verify dengan status Failed, duplicate verify (409)
validation/booking.test.ts	6 schemas	5	acceptAssignmentSchema 0 test
shared/date.test.ts	5 functions	3	isBookingPast, isWithinHours 0 test
Perbandingan dengan docs/testing/test-cases.md
Dari ~40+ test case di dokumen yang relevan untuk unit test API, hanya ~30% yang benar-benar dicover. Contoh yang tidak dicover:
- BOOK-006 (tanggal sebelum hari ini), BOOK-010 (double submit), BOOK-014 (booking number unique)
- AUTH-006 (email invalid format), AUTH-008 (session expired), AUTH-010 (role admin dashboard)
- CUST-015 s.d. CUST-030 (sebagian besar tidak ada test)
4. Kesenjangan Konfigurasi & Infrastruktur
Item	Status
Coverage reporter	Tidak ada satu pun vitest.config.ts yang define coverage
globalSetup/globalTeardown	Tidak ada
Test di CI	Tidak ada workflow GitHub Actions / CI config untuk menjalankan test
Test di apps/web	Tidak ada test script di package.json
Test di apps/cms	Tidak ada test script
Test di packages/{database,config,types,ui}	Tidak ada test script
Testing Library	Tidak terinstal di package mana pun
Playwright	Tidak terinstal
supertest	Terinstal di api tapi tidak dipakai (Hono app.request() digunakan)
test script di turbo.json	dependsOn: ["^build"] — padahal test seharusnya tidak perlu build dulu untuk unit test
5. Pelanggaran Testing Strategy
testing-strategy.md baris 851: "Database tidak boleh dimock pada Integration Test"
Realita: Semua test API mem-mock seluruh database (vi.mock('../lib/db.ts')). Jadi test yang ada saat ini adalah unit test dengan mock berat, bukan integration test sesuai definisi dokumen.
Ringkasan Prioritas Perbaikan
Prioritas	Area	Estimasi Effort
P0	Test apps/api/src/lib/ (auth, audit, email, notification, redis, storage, cms)	3-4 hari
P0	Test apps/api/src/middleware/ (auth, error-handler, rate-limiter)	1 hari
P1	Tambah coverage endpoints yang missing di route test yang sudah ada	3-4 hari
P1	Response body assertion + mock verification di semua test API	2 hari
P2	Setup coverage config dan target 80%	0.5 hari
P2	Test packages/database/ (schema integrity, relations, client)	1-2 hari
P2	Test packages/ui/ (13 komponen dengan Testing Library)	2-3 hari
P3	Setup Playwright + implementasi E2E flow P0	5-7 hari
P3	Setup CI workflow untuk test otomatis	1 hari
P4	Test apps/web/ (integration test halaman)	5-7 hari
P4	True integration test (DB real, tanpa mock db)	3-5 hari
Apakah Anda ingin saya mendetailkan salah satu area, atau menyusun rencana implementasi untuk prioritas tertentu?
▣  Plan · DeepSeek V4 Flash Free · 5m 35s
susun rencana untuk semua implementasi sesuai prioritas dan agar unit test sudah benar benar ter implementasi dengan benar dalam platform ini mau itu vitest, supertest, playwright, dll
+ Thought: 1.6s
✓Explore Task — Explore web & UI test deps
↳ 9 toolcalls · 24.5s
✓Explore Task — Explore lib source files deeply
↳ 7 toolcalls · 38.7s
✓Explore Task — Explore DB, web structure
↳ 13 toolcalls · 45.8s
ctrl+x down view subagents
+ Thought: 9.7s
Sekarang saya punya pemahaman yang sangat komprehensif. Berikut rencana implementasi lengkap berdasarkan prioritas, dengan mempertimbangkan stack teknologinya (Vitest, Supertest, Testing Library, Playwright) dan kondisi aktual codebase.
Rencana Implementasi Testing — Spesialis Platform
Prinsip & Pendekatan
Layer	Tools	Approach
Pure unit (lib/, shared, validation)	Vitest	Mock dependency eksternal (nodemailer, redis, db), test fungsi murni
Route/API (routes/)	Vitest + Hono app.request()	Mock db + auth + email + notifikasi, test status + response body + side effect
Middleware (middleware/)	Vitest	Mock Hono Context, test tiap middleware secara terisolasi
Schema/Database (database/)	Vitest	Test ekspor schema, relasi, helper — tanpa koneksi DB
UI Component (packages/ui/)	Vitest + Testing Library + jsdom	Render, interaction, aksesibilitas — 13 komponen
Web Component (apps/web/src/components/)	Vitest + Testing Library + jsdom	Render komponen React interaktif di web
E2E	Playwright	Docker compose up → test flow P0 di browser nyata
Integration	Vitest + PostgreSQL testcontainers	Test endpoint dengan DB real, tanpa mock db
Fase 0 — Foundation & Perbaikan Infrastructure
Target: Semua package punya test runner, coverage config, dan test yang ada sudah berkualitas minimal.
0.1 Setup Vitest di package yang belum punya
Package	Action
packages/database	Add vitest devDep + vitest.config.ts + "test": "vitest run" script
packages/ui	Add vitest devDep + vitest.config.ts (with jsdom) + "test": "vitest run" + @testing-library/react, @testing-library/jest-dom, jsdom
apps/web	Add vitest devDep + vitest.config.ts (with jsdom) + "test": "vitest run" + @testing-library/react, @testing-library/jest-dom, jsdom
0.2 Tambah Coverage Config di Semua vitest.config.ts
// template untuk tiap vitest.config.ts
test: {
  include: ['src/**/*.test.ts'],
  coverage: {
    provider: 'v8',
    reporter: ['text', 'lcov', 'html'],
    include: ['src/**'],
    exclude: ['src/**/*.test.ts', 'src/**/index.ts'],
    thresholds: {
      statements: 80,
      branches: 75,
      functions: 80,
      lines: 80,
    },
  },
}
Affected files: apps/api/vitest.config.ts, packages/shared/vitest.config.ts, packages/validation/vitest.config.ts, dan semua yang baru dibuat.
0.3 Perbaiki Test API yang Ada (Quality Fix)
Issue	Files	Action
Response body never inspected	Semua route test	Tambah const res = await ... → const body = await res.json() → expect(body.success).toBe(true) dll
Mock never verified	Semua route test	Tambah expect(mockAudit.createAuditLog).toHaveBeenCalled() di setiap test yang trigger audit
console.error debug artifact	bookings.test.ts:292-295	Hapus console.error('Assign test body:', ...)
Rate limiter never tested	Semua route test	Minimal 1 test per route file yang verifikasi 429 behavior
0.4 Update turbo.json
Ubah "test": { "dependsOn": ["^build"] } → "test": {} — unit test tidak perlu build dulu. Simpan dependsOn: ["^build"] hanya untuk package yang benar-benar butuh build (seperti packages/ui yang perlu transpile JSX).
Fase 1 — Unit Test: lib/ + middleware/ (P0)
Target: Coverage 100% di lib/ dan middleware/ — 12 file, ~500 baris source.
1.1 apps/api/src/lib/auth.test.ts — 8 fungsi, ~80 baris source
Test	Approach
hashPassword(password)	Mock argon2id dari hash-wasm, verify return string encoded
verifyPassword(hash, password) — valid	Mock argon2id return hash yang sama, expect true
verifyPassword(hash, password) — invalid	Mock argon2id return hash berbeda, expect false
verifyPassword — malformed hash	Input hash tanpa 6 segment, expect false
signAccessToken(userId, role)	Mock sign dari hono/jwt, verify dipanggil dengan payload correct
verifyAccessToken(token) — valid	Mock verify return payload, verify sub + role
verifyAccessToken(token) — expired	Mock verify throw error, expect reject
generateRefreshToken()	Verify return UUID string
hashToken(token)	Verify return SHA-256 hex string (panjang 64)
getRefreshTokenExpiry()	Verify return Date 7 hari ke depan
1.2 apps/api/src/lib/audit.test.ts — 1 fungsi, ~25 baris source
Test	Approach
createAuditLog with all fields	Mock db.insert(auditLogs).values(...), verify values correct
createAuditLog with IP from header	Mock c.req.header('x-forwarded-for')
createAuditLog with null oldValue/newValue	Verify null di DB
1.3 apps/api/src/lib/email.test.ts — 7 fungsi, ~265 baris source
Test	Approach
sendPasswordResetEmail	Mock nodemailer.createTransport().sendMail(), verify to, subject, text contains reset URL
sendBookingConfirmationEmail	Verify contains booking number, tracking URL
sendPartnerAssignedEmail	Verify contains booking number, dashboard URL
sendPartnerVerifiedEmail — Approved	Verify subject contains "Disetujui"
sendPartnerVerifiedEmail — Rejected	Verify subject contains "Ditolak"
sendVerificationEmail	Verify contains verify URL
sendPaymentVerifiedEmail — Paid/Failed	Verify subject berbeda
sendNotificationEmail	Verify generic title + message
Error handling	Mock sendMail throw → verify console.error called (atau spy)
1.4 apps/api/src/lib/notification.test.ts — 2 fungsi, ~44 baris source
Test	Approach
createNotification with channel 'In App'	Mock db.insert, verify values, verify sendNotificationEmail NOT called
createNotification with channel 'Email'	Verify sendNotificationEmail called with correct params
createNotification with channel 'WhatsApp'	Mock sendWhatsApp, verify called
notifyAdmins	Mock db.select().from(users).where() return list admin, verify createNotification called per admin
1.5 apps/api/src/lib/redis.test.ts — 1 fungsi, ~55 baris source
Test	Approach
getRedis() — first call	Mock ioredis.Redis constructor, verify lazy init
getRedis() — second call	Verify return same instance (singleton)
getRedis() — Redis unavailable	Mock constructor throw, verify return null
getRedis() — DNS lookup fail	Mock dns.lookup reject, verify return null + warning log
1.6 apps/api/src/lib/storage.test.ts — 5 fungsi, ~60 baris source
Test	Approach
isAllowedMimeType — valid	image/jpeg, image/png, image/webp, application/pdf → true
isAllowedMimeType — invalid	text/html, application/json → false
isWithinSizeLimit — within	5MB → true
isWithinSizeLimit — exceed	11MB → false
ensureUploadDir — exists	Mock existsSync return true → verify mkdir NOT called
ensureUploadDir — not exists	Mock existsSync return false → verify mkdir called
saveFile	Mock writeFile, verify return StoredFile with correct shape
deleteFile	Mock unlink, verify called with correct path
1.7 apps/api/src/lib/cms.test.ts — 1 objek, ~81 baris source
Test	Approach
Mock globalThis.fetch, test tiap method (faq.list, articles.list, dll) return data dengan format benar	 
Test error handling saat CMS down (fetch throw → promise reject)	 
1.8 apps/api/src/middleware/auth.test.ts — 2 fungsi, ~35 baris source
Test	Approach
authMiddleware — valid Bearer token	Mock c.req.header('Authorization') return 'Bearer valid-token', mock verifyAccessToken return payload, verify c.set called
authMiddleware — no token	Mock header return undefined, verify unauthorized called
authMiddleware — invalid token	Mock verifyAccessToken throw, verify unauthorized called with "Invalid or expired"
authMiddleware — token from cookie	Mock header undefined, mock getCookie return token, verify success
requireRole — allowed role	Set c.get('userRole') return 'admin', call with roles=['admin'], verify next() called
requireRole — forbidden role	Set c.get('userRole') return 'customer', call with roles=['admin'], verify forbidden called
requireRole — multiple allowed roles	Set role 'dispatcher', verify allowed
1.9 apps/api/src/middleware/error-handler.test.ts — ~20 baris source
Test	Approach
errorHandler with HTTPException	Create HTTPException(404, 'Not found'), verify response JSON with status 404
errorHandler with generic Error	Mock console.error, verify response 500 via serverError
errorHandler with userId in context	Mock c.get('userId'), verify log includes userId
1.10 apps/api/src/middleware/rate-limiter.test.ts — ~88 baris source
Test	Approach
rateLimit() — first request	Verify next() called (not limited)
rateLimit() — exceed limit	Call 21× in same window, verify return 429
rateLimit() — window reset	Call 21×, wait mock timer, call again → 200 (not limited)
rateLimit() — different IP	21× from IP A limited, IP B not limited
rateLimit() — Redis path	Mock getRedis() return mock Redis, verify Redis rate limiting path
Fase 2 — Perbaiki Test Route yang Ada (P1)
Target: Response body + mock assertion + missing endpoints.
2.1 Tambah Response Body Assertion
Di semua file route test, ubah pattern:
// FROM (cuma cek status code)
const res = await app.request('/path');
expect(res.status).toBe(200);

// TO (cek body juga)
const res = await app.request('/path');
const body = await res.json() as ApiTestResponse<...>;
expect(res.status).toBe(200);
expect(body.success).toBe(true);
expect(body.data).toBeDefined();
2.2 Tambah Mock Verification
Di test yang trigger side effect (audit log, email, notifikasi), tambah:
expect(mockAudit.createAuditLog).toHaveBeenCalledWith(
  expect.anything(),
  expect.objectContaining({ action: 'booking.confirm' }),
);
2.3 Tambah Missing Endpoints
Route file	Endpoint ke-cover
bookings.test.ts	GET /:id, POST /:id/reject, POST /:id/on-the-way, POST /:id/start, POST /:id/complete
auth.test.ts	POST /convert-guest, POST /verify-email, POST /resend-verification
payments.test.ts	Verify dengan status Failed, duplicate verify (409)
2.4 Tambah Edge Case dari docs/testing/test-cases.md
Prioritas sesuai dokumen (P0 > P1 > P2):
- BOOK-006: Tanggal sebelum hari ini → validation error
- BOOK-010: Double submit → cuma 1 order
- BOOK-015: Nomor HP Indonesia valid
- AUTH-006: Email invalid format
- AUTH-008: Session expired → redirect login
Fase 3 — Database Schema Integrity (P2)
Target: 37 file schema terverifikasi.
packages/database/src/schema.test.ts
Test	Approach
Setiap tabel terekspor dari schema/index.ts	import * as schema from './schema', verify 32+ key exists
Setiap tabel punya id kolom UUID	Verify pgTable result has id with .primaryKey()
Relasi terekspor dari relations.ts	Verify relations() dipanggil untuk tabel2 yang punya relasi
Tabel soft-delete punya deletedAt	Scan kolom untuk deletedAt
Enum types match @specialist/types	Verify $type<UserRole>() dll
client.ts export db	Verify drizzle() dipanggil dengan correct config
Ini test tanpa koneksi DB — cukup import dan verify struktur objek.
Fase 4 — UI Component Tests (P2)
Target: 13 komponen di packages/ui/src/components/.
Infrastructure
- packages/ui/vitest.config.ts dengan environment: 'jsdom'
- packages/ui/src/test-setup.ts dengan @testing-library/jest-dom (auto cleanup)
- packages/ui/package.json: tambah "test": "vitest run", "test:watch": "vitest"
Komponen & Test Per Komponen
Komponen	Test Cases
Button	render default, render with label, disabled state, click handler, variant (primary/secondary/outline/danger), loading state
Input	render, placeholder, value, onChange, error state, disabled, label, type variants (text/password/email/number)
Select	render options, value selection, onChange, placeholder option, disabled
Textarea	render, value, onChange, rows, maxLength, placeholder, disabled
FileUpload	render, file select, accept types, max size, multiple files, drag-and-drop (jika ada)
DatePicker	render, date select, min/max date, disabled, format display
Pagination	render page numbers, prev/next button, active page, disabled prev on first page, disabled next on last page, onPageChange
Table	render headers, render rows, empty state, sortable header, loading skeleton
Modal	render closed, render open, close on overlay click, close on Escape, close button, prevent scroll, children render
Badge	render text, variant (success/warning/error/info), small/medium/large
Card	render title, render children, shadow variant, padding variant, with/without border
Container	render children, max-width variant (sm/md/lg/xl), padding variant
Typography	render h1-h6, paragraph, span, variant (heading/body/caption), color variant
Fase 5 — Web Component Tests (P3)
Target: 27 komponen React di apps/web/src/components/.
Infrastructure
- apps/web/vitest.config.ts dengan environment: 'jsdom' + setupFiles
- apps/web/src/test-setup.ts dengan @testing-library/jest-dom
- apps/web/package.json: tambah "test": "vitest run"
Prioritas Komponen (berdasarkan docs/testing/testing-strategy.md)
Prioritas	Komponen	Test
P0	LoginForm, RegisterForm, BookingForm, AuthGuard	Render, validation, submit flow, error state, role guard logic
P1	ForgotPasswordForm, ResetPasswordForm, CorporateInquiryForm, PartnerRegistrationForm, VerifyEmailForm	Form validation, API call mocks
P2	ServiceList, TrackingForm, CustomerOverview, PartnerOverview, AdminOverview	Render data, empty state, loading skeleton
P3	Semua komponen dashboard customer/partner/admin/corporate lainnya	Render, interaction, edge cases
Fase 6 — Perbaiki Test Validation & Shared (P3)
packages/validation/src/booking.test.ts
Tambah test untuk:
- acceptAssignmentSchema — 0 test saat ini
- mediaIdsSchema — UUID array valid, invalid UUID, >10 items
- Semua boundary value: min(1), max(255), min(3), max(500), dll
- phone regex (^\+?[0-9]+$)
- bookingDate regex pattern
- bookingTime regex pattern
packages/shared/src/helpers/date.test.ts
Tambah test untuk:
- isBookingPast — 0 test saat ini
- isWithinHours — 0 test saat ini
- parseBookingDateTime — invalid input
- getDateRange — verifikasi tanggal spesifik (bukan cuma regex)
Semua file validation test
Tambah assertion result.error?.issues untuk verifikasi field name dan error message spesifik.
Fase 7 — Playwright E2E (P3)
Infrastructure
1. Install Playwright: pnpm add -D @playwright/test di root
2. playwright.config.ts di root
3. Docker profile test di docker-compose.yml (bootstrap DB + API + Web)
4. Script seed data khusus test
5. GitHub Actions workflow untuk E2E
Flow yang Diimplementasi (prioritas P0 dari docs/testing/e2e-flows.md)
Flow ID	Flow	Pages to test
E2E-001	Guest Booking → Admin Dashboard → Booking muncul	/ → Booking → Submit → Admin Dashboard
E2E-002	Guest Tracking → Booking number → Timeline	/tracking → Input BN → Verify
E2E-003	Customer Login → Dashboard → History → Profile → Logout	Full auth flow
E2E-006	Partner Login → Dashboard → Assignment	Partner dashboard
E2E-014	Admin Login → Dashboard	Admin dashboard
E2E-015	Admin Assign Partner	Booking → Assign → Save
E2E-022	Search → Result → Service Detail	/ → Search → Click → Detail
E2E-026	Permission Test — Customer buka /admin → 403	Role guard
Cross-Module	Full lifecycle: Booking → Assign → Accept → Work → Complete → Payment → Verify → Review	End-to-end
Fase 8 — True Integration Tests (P4)
Target: Test endpoint dengan database real (PostgreSQL) — sesuai testing-strategy.md line 851.
Infrastructure
- testcontainers untuk PostgreSQL di CI
- Script db:test:seed dengan data fixture
- File apps/api/src/routes/*.integration.test.ts (pisah dari unit test)
- Jalankan hanya via pnpm test:integration (bukan default pnpm test)
Yang di-test dengan DB real
- Booking state machine (transitions, constraints)
- Unique constraints (email, phone, booking number)
- Transaction rollback (concurrent booking requests)
- Cascade delete behavior
- Rating/aggregate queries (ORDER BY, GROUP BY)
Ringkasan Timeline & Effort
Fase	Area	File Count	Test Count (est.)	Effort
F0	Infra & Fixes	~12 config	—	2 hari
F1	lib/ + middleware/ test	12 test files	~60 tests	5 hari
F2	Route test perbaikan	~15 files	~200 assertions added	4 hari
F3	Database schema test	1 test file	~20 tests	1 hari
F4	UI component test	13 test files	~80 tests	4 hari
F5	Web component test	~15 test files	~90 tests	5 hari
F6	Validation & shared perbaikan	~12 files	~60 tests	2 hari
F7	Playwright E2E	~12 spec files	~30 flows	7 hari
F8	True integration test	~8 test files	~40 tests	5 hari
Total	 	 	~580 tests	~35 hari
# Questions
Apakah Anda setuju dengan pendekatan 8 fase ini? Ada penyesuaian prioritas atau area yang ingin difokuskan lebih dulu?
Setuju, lanjutkan
Untuk Fase 7 (Playwright E2E), bagaimana preferensi environment testing?
Docker compose test profile
Untuk Fase 0.3 (perbaiki test API yang ada), apakah saya perlu detailkan setiap perubahan per file sekarang, atau langsung implement sesuai pola?
Langsung implement sesuai pola
+ Thought: 2.4s
Terima kasih konfirmasinya. Berikut rencana final yang sudah disesuaikan:
Rencana Final — Testing Spesialis Platform
Prioritas: Fase 0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8
Fase 0 — Foundation (2 hari)
Task	File yang dibuat/dimodifikasi
Setup Vitest di packages/database	packages/database/vitest.config.ts, package.json + "test" script, vitest devDep
Setup Vitest + Testing Library di packages/ui	packages/ui/vitest.config.ts (jsdom), package.json + "test" script, vitest + @testing-library/react + @testing-library/jest-dom + jsdom
Setup Vitest + Testing Library di apps/web	Sama seperti ui
Tambah coverage config di semua vitest.config	apps/api/, packages/shared/, packages/validation/, + yang baru
Perbaiki test API existing	Semua route test: tambah res.json() assertion + mock verification + hapus console.error
Update turbo.json	"test": {} tanpa dependsOn: ["^build"]
Fase 1 — lib/ + middleware/ Tests (5 hari)
12 test files baru — detail per-file sudah dijelaskan di rencana sebelumnya. Ringkasan:
File	Source baris	Fungsi	Test count
apps/api/src/lib/auth.test.ts	79	8 fungsi auth	~12
apps/api/src/lib/audit.test.ts	25	1 fungsi	~4
apps/api/src/lib/email.test.ts	265	7 fungsi email	~15
apps/api/src/lib/notification.test.ts	44	2 fungsi	~6
apps/api/src/lib/redis.test.ts	55	1 fungsi	~5
apps/api/src/lib/storage.test.ts	60	5 fungsi	~10
apps/api/src/lib/cms.test.ts	81	1 objek API client	~8
apps/api/src/middleware/auth.test.ts	35	2 middleware	~8
apps/api/src/middleware/error-handler.test.ts	20	1 handler	~4
apps/api/src/middleware/rate-limiter.test.ts	88	1 factory	~8
Fase 2 — Route Test Enhancement (4 hari)
Perbaiki ~15 file route test yang sudah ada:
- Tambah res.json() assertion di semua test
- Tambah mock verification (toHaveBeenCalled)
- Tambah missing endpoints: GET /:id, POST /:id/reject, POST /convert-guest, POST /verify-email, dll
- Tambah edge case dari docs/testing/test-cases.md (BOOK-006, BOOK-010, BOOK-015, AUTH-006, AUTH-008)
Fase 3 — Database Schema Test (1 hari)
1 file: packages/database/src/schema.test.ts
- Verifikasi 32 tabel + relations terekspor dengan benar
- Verifikasi primary key, soft delete, enum types
Fase 4 — UI Component Tests (4 hari)
13 test files di packages/ui/src/components/*.test.tsx
- Button, Input, Select, Textarea, FileUpload, DatePicker, Pagination
- Table, Modal, Badge, Card, Container, Typography
- Render, interaction, accessibility, variants, states
Fase 5 — Web Component Tests (5 hari)
~15 test files di apps/web/src/components/*.test.tsx
- Prioritas P0: LoginForm, RegisterForm, BookingForm, AuthGuard
- Prioritas P1: ForgotPasswordForm, ResetPasswordForm, CorporateInquiryForm, dll
- Prioritas P2: ServiceList, TrackingForm, dashboard overviews
Fase 6 — Validation & Shared Enhancement (2 hari)
- validation/src/booking.test.ts: tambah acceptAssignmentSchema, mediaIdsSchema, boundary value untuk semua field
- shared/src/helpers/date.test.ts: tambah isBookingPast, isWithinHours, specific date verification
- Semua validation test: tambah assertion result.error?.issues untuk field name spesifik
Fase 7 — Playwright E2E (7 hari)
Environment: Docker compose test profile (docker compose --profile test up)
- Seed data khusus test
- API + Web + PostgreSQL running
12 spec files mencakup 30 flow:
Prioritas	Flow
P0	Guest Booking + Admin Dashboard verify (E2E-001)
P0	Guest Tracking (E2E-002)
P0	Customer Login → Dashboard → History → Profile → Logout (E2E-003)
P0	Partner Login → Dashboard → Assignment (E2E-006)
P0	Admin Login → Dashboard (E2E-014)
P0	Admin Assign Partner (E2E-015)
P0	Cross-module lifecycle (Booking → Assign → Accept → Work → Complete → Payment → Review)
P1	Customer Repeat Booking (E2E-004), Customer Review (E2E-005)
P1	Partner Accept/Reject/Complete (E2E-007, E2E-008, E2E-009)
P1	Permission test (E2E-026), Forgot Password (E2E-025)
P1	Responsive test (E2E-028), Accessibility test (E2E-029)
P2	Search (E2E-022), Corporate flows (E2E-010–013)
Fase 8 — True Integration Tests (5 hari)
~8 file *.integration.test.ts dengan PostgreSQL real via Testcontainers
- Booking state machine transitions
- Unique constraint violations (email, phone, booking number)
- Transaction rollback / concurrent requests
- Cascade delete, aggregate queries
Ringkasan Total: ~580 test, ~35 hari
Fase	Test count	File count	Effort
F0 - Foundation	—	~12	2 hari
F1 - lib & middleware	~80	12	5 hari
F2 - Route enhancement	~200	15	4 hari
F3 - Database schema	~20	1	1 hari
F4 - UI components	~80	13	4 hari
F5 - Web components	~90	15	5 hari
F6 - Validation & shared	~60	12	2 hari
F7 - Playwright E2E	~30	12	7 hari
F8 - True integration	~40	8	5 hari
Total	~580	~100	~35 hariBerikut laporan lengkap hasil observasi terhadap dokumentasi, infrastruktur, dan implementasi testing platform:
Ringkasan Eksekutif
Dari ~14 package/app, hanya 3 yang memiliki test (api, shared, validation). Coverage kode < 20% overall. Banyak area yang memiliki dokumentasi testing lengkap tapi nol implementasi.
1. Kesenjangan Dokumentasi vs Implementasi
Dokumen	Isi	Realita
docs/testing/testing-strategy.md	Target coverage 80%, Stack: Vitest + Testing Library + Playwright + Supertest	Coverage ~15-20%, Testing Library tidak terinstal, Playwright tidak ada, Supertest di package.json tapi tidak dipakai
docs/testing/e2e-flows.md	30 E2E flow (P0-P2), Playwright, multi-browser	Nol implementasi — tidak ada file Playwright satupun
docs/testing/test-cases.md (Part 1 & 2)	~150+ test case skenario (AUTH, BOOK, CUST, ADMIN, dll)	Banyak skenario belum dicover oleh unit test (lihat detail di bawah)
manual-test.md	128 manual test case, 19 area	Nol automasi — semua manual, belum ada satupun tercentang
2. File/Layer Tanpa Test Sama Sekali
apps/api/src/lib/ — 9 dari 11 file tanpa test (18% coverage)
File	Fungsi
auth.ts	JWT sign/verify, Argon2 hash/verify, refresh token
audit.ts	Audit log creation
cms.ts	Directus sync / cache
db.ts	Drizzle client init
email.ts	Nodemailer transport
email-templates.ts	Template HTML email
notification.ts	In-app + WhatsApp notification
redis.ts	Redis client & caching
storage.ts	File upload (local/R2)
apps/api/src/middleware/ — 3 dari 3 file tanpa test (0% coverage)
File	Fungsi
auth.ts	Auth middleware + RBAC requireRole
error-handler.ts	Global error handler
rate-limiter.ts	Rate limiting
packages/database/ — 36 file tanpa test (0% coverage)
- 32 table schemas, relations, client, seeds — tidak ada satu test pun
packages/ui/ — 13 komponen tanpa test (0% coverage)
- Button, Modal, Input, Table, DatePicker, dll — padahal testing-strategy.md mewajibkan component test
apps/web/ — ~30 halaman tanpa test (0% coverage)
- Tidak ada Vitest, tidak ada Playwright, tidak ada test framework sama sekali
3. Kesenjangan Kualitas pada Test yang Ada
Masalah Sistematis (semua file test API)
1. Response body tidak pernah diperiksa — Semua test cuma cek res.status, tidak ada yang baca res.json() untuk verifikasi data, pesan error, token dll
2. Mock tidak diverifikasi — createAuditLog, createNotification, sendEmail dll dipanggil di production code tapi test tidak pernah ngecek toHaveBeenCalled()
3. Rate limiter selalu di-skip — Mock rate limiter no-op, enforcement tidak pernah diuji
4. Edge case validation tidak ada — Boundaries, null/undefined, format invalid jarang diuji
Kesenjangan Per Endpoint (yang sudah ada test)
Route File	Endpoints di Source	Covered	Missing
bookings.test.ts	~15 endpoints	~11	GET /:id, POST /:id/reject, POST /:id/on-the-way, POST /:id/start, POST /:id/complete
auth.test.ts	~11 endpoints	~8	POST /convert-guest, POST /verify-email, POST /resend-verification
payments.test.ts	~4 endpoints	~3	Verify dengan status Failed, duplicate verify (409)
validation/booking.test.ts	6 schemas	5	acceptAssignmentSchema 0 test
shared/date.test.ts	5 functions	3	isBookingPast, isWithinHours 0 test
Perbandingan dengan docs/testing/test-cases.md
Dari ~40+ test case di dokumen yang relevan untuk unit test API, hanya ~30% yang benar-benar dicover. Contoh yang tidak dicover:
- BOOK-006 (tanggal sebelum hari ini), BOOK-010 (double submit), BOOK-014 (booking number unique)
- AUTH-006 (email invalid format), AUTH-008 (session expired), AUTH-010 (role admin dashboard)
- CUST-015 s.d. CUST-030 (sebagian besar tidak ada test)
4. Kesenjangan Konfigurasi & Infrastruktur
Item	Status
Coverage reporter	Tidak ada satu pun vitest.config.ts yang define coverage
globalSetup/globalTeardown	Tidak ada
Test di CI	Tidak ada workflow GitHub Actions / CI config untuk menjalankan test
Test di apps/web	Tidak ada test script di package.json
Test di apps/cms	Tidak ada test script
Test di packages/{database,config,types,ui}	Tidak ada test script
Testing Library	Tidak terinstal di package mana pun
Playwright	Tidak terinstal
supertest	Terinstal di api tapi tidak dipakai (Hono app.request() digunakan)
test script di turbo.json	dependsOn: ["^build"] — padahal test seharusnya tidak perlu build dulu untuk unit test
5. Pelanggaran Testing Strategy
testing-strategy.md baris 851: "Database tidak boleh dimock pada Integration Test"
Realita: Semua test API mem-mock seluruh database (vi.mock('../lib/db.ts')). Jadi test yang ada saat ini adalah unit test dengan mock berat, bukan integration test sesuai definisi dokumen.
Ringkasan Prioritas Perbaikan
Prioritas	Area	Estimasi Effort
P0	Test apps/api/src/lib/ (auth, audit, email, notification, redis, storage, cms)	3-4 hari
P0	Test apps/api/src/middleware/ (auth, error-handler, rate-limiter)	1 hari
P1	Tambah coverage endpoints yang missing di route test yang sudah ada	3-4 hari
P1	Response body assertion + mock verification di semua test API	2 hari
P2	Setup coverage config dan target 80%	0.5 hari
P2	Test packages/database/ (schema integrity, relations, client)	1-2 hari
P2	Test packages/ui/ (13 komponen dengan Testing Library)	2-3 hari
P3	Setup Playwright + implementasi E2E flow P0	5-7 hari
P3	Setup CI workflow untuk test otomatis	1 hari
P4	Test apps/web/ (integration test halaman)	5-7 hari
P4	True integration test (DB real, tanpa mock db)	3-5 hari
Apakah Anda ingin saya mendetailkan salah satu area, atau menyusun rencana implementasi untuk prioritas tertentu?
▣  Plan · DeepSeek V4 Flash Free · 5m 35s
susun rencana untuk semua implementasi sesuai prioritas dan agar unit test sudah benar benar ter implementasi dengan benar dalam platform ini mau itu vitest, supertest, playwright, dll
+ Thought: 1.6s
✓Explore Task — Explore web & UI test deps
↳ 9 toolcalls · 24.5s
✓Explore Task — Explore lib source files deeply
↳ 7 toolcalls · 38.7s
✓Explore Task — Explore DB, web structure
↳ 13 toolcalls · 45.8s
ctrl+x down view subagents
+ Thought: 9.7s
Sekarang saya punya pemahaman yang sangat komprehensif. Berikut rencana implementasi lengkap berdasarkan prioritas, dengan mempertimbangkan stack teknologinya (Vitest, Supertest, Testing Library, Playwright) dan kondisi aktual codebase.
Rencana Implementasi Testing — Spesialis Platform
Prinsip & Pendekatan
Layer	Tools	Approach
Pure unit (lib/, shared, validation)	Vitest	Mock dependency eksternal (nodemailer, redis, db), test fungsi murni
Route/API (routes/)	Vitest + Hono app.request()	Mock db + auth + email + notifikasi, test status + response body + side effect
Middleware (middleware/)	Vitest	Mock Hono Context, test tiap middleware secara terisolasi
Schema/Database (database/)	Vitest	Test ekspor schema, relasi, helper — tanpa koneksi DB
UI Component (packages/ui/)	Vitest + Testing Library + jsdom	Render, interaction, aksesibilitas — 13 komponen
Web Component (apps/web/src/components/)	Vitest + Testing Library + jsdom	Render komponen React interaktif di web
E2E	Playwright	Docker compose up → test flow P0 di browser nyata
Integration	Vitest + PostgreSQL testcontainers	Test endpoint dengan DB real, tanpa mock db
Fase 0 — Foundation & Perbaikan Infrastructure
Target: Semua package punya test runner, coverage config, dan test yang ada sudah berkualitas minimal.
0.1 Setup Vitest di package yang belum punya
Package	Action
packages/database	Add vitest devDep + vitest.config.ts + "test": "vitest run" script
packages/ui	Add vitest devDep + vitest.config.ts (with jsdom) + "test": "vitest run" + @testing-library/react, @testing-library/jest-dom, jsdom
apps/web	Add vitest devDep + vitest.config.ts (with jsdom) + "test": "vitest run" + @testing-library/react, @testing-library/jest-dom, jsdom
0.2 Tambah Coverage Config di Semua vitest.config.ts
// template untuk tiap vitest.config.ts
test: {
  include: ['src/**/*.test.ts'],
  coverage: {
    provider: 'v8',
    reporter: ['text', 'lcov', 'html'],
    include: ['src/**'],
    exclude: ['src/**/*.test.ts', 'src/**/index.ts'],
    thresholds: {
      statements: 80,
      branches: 75,
      functions: 80,
      lines: 80,
    },
  },
}
Affected files: apps/api/vitest.config.ts, packages/shared/vitest.config.ts, packages/validation/vitest.config.ts, dan semua yang baru dibuat.
0.3 Perbaiki Test API yang Ada (Quality Fix)
Issue	Files	Action
Response body never inspected	Semua route test	Tambah const res = await ... → const body = await res.json() → expect(body.success).toBe(true) dll
Mock never verified	Semua route test	Tambah expect(mockAudit.createAuditLog).toHaveBeenCalled() di setiap test yang trigger audit
console.error debug artifact	bookings.test.ts:292-295	Hapus console.error('Assign test body:', ...)
Rate limiter never tested	Semua route test	Minimal 1 test per route file yang verifikasi 429 behavior
0.4 Update turbo.json
Ubah "test": { "dependsOn": ["^build"] } → "test": {} — unit test tidak perlu build dulu. Simpan dependsOn: ["^build"] hanya untuk package yang benar-benar butuh build (seperti packages/ui yang perlu transpile JSX).
Fase 1 — Unit Test: lib/ + middleware/ (P0)
Target: Coverage 100% di lib/ dan middleware/ — 12 file, ~500 baris source.
1.1 apps/api/src/lib/auth.test.ts — 8 fungsi, ~80 baris source
Test	Approach
hashPassword(password)	Mock argon2id dari hash-wasm, verify return string encoded
verifyPassword(hash, password) — valid	Mock argon2id return hash yang sama, expect true
verifyPassword(hash, password) — invalid	Mock argon2id return hash berbeda, expect false
verifyPassword — malformed hash	Input hash tanpa 6 segment, expect false
signAccessToken(userId, role)	Mock sign dari hono/jwt, verify dipanggil dengan payload correct
verifyAccessToken(token) — valid	Mock verify return payload, verify sub + role
verifyAccessToken(token) — expired	Mock verify throw error, expect reject
generateRefreshToken()	Verify return UUID string
hashToken(token)	Verify return SHA-256 hex string (panjang 64)
getRefreshTokenExpiry()	Verify return Date 7 hari ke depan
1.2 apps/api/src/lib/audit.test.ts — 1 fungsi, ~25 baris source
Test	Approach
createAuditLog with all fields	Mock db.insert(auditLogs).values(...), verify values correct
createAuditLog with IP from header	Mock c.req.header('x-forwarded-for')
createAuditLog with null oldValue/newValue	Verify null di DB
1.3 apps/api/src/lib/email.test.ts — 7 fungsi, ~265 baris source
Test	Approach
sendPasswordResetEmail	Mock nodemailer.createTransport().sendMail(), verify to, subject, text contains reset URL
sendBookingConfirmationEmail	Verify contains booking number, tracking URL
sendPartnerAssignedEmail	Verify contains booking number, dashboard URL
sendPartnerVerifiedEmail — Approved	Verify subject contains "Disetujui"
sendPartnerVerifiedEmail — Rejected	Verify subject contains "Ditolak"
sendVerificationEmail	Verify contains verify URL
sendPaymentVerifiedEmail — Paid/Failed	Verify subject berbeda
sendNotificationEmail	Verify generic title + message
Error handling	Mock sendMail throw → verify console.error called (atau spy)
1.4 apps/api/src/lib/notification.test.ts — 2 fungsi, ~44 baris source
Test	Approach
createNotification with channel 'In App'	Mock db.insert, verify values, verify sendNotificationEmail NOT called
createNotification with channel 'Email'	Verify sendNotificationEmail called with correct params
createNotification with channel 'WhatsApp'	Mock sendWhatsApp, verify called
notifyAdmins	Mock db.select().from(users).where() return list admin, verify createNotification called per admin
1.5 apps/api/src/lib/redis.test.ts — 1 fungsi, ~55 baris source
Test	Approach
getRedis() — first call	Mock ioredis.Redis constructor, verify lazy init
getRedis() — second call	Verify return same instance (singleton)
getRedis() — Redis unavailable	Mock constructor throw, verify return null
getRedis() — DNS lookup fail	Mock dns.lookup reject, verify return null + warning log
1.6 apps/api/src/lib/storage.test.ts — 5 fungsi, ~60 baris source
Test	Approach
isAllowedMimeType — valid	image/jpeg, image/png, image/webp, application/pdf → true
isAllowedMimeType — invalid	text/html, application/json → false
isWithinSizeLimit — within	5MB → true
isWithinSizeLimit — exceed	11MB → false
ensureUploadDir — exists	Mock existsSync return true → verify mkdir NOT called
ensureUploadDir — not exists	Mock existsSync return false → verify mkdir called
saveFile	Mock writeFile, verify return StoredFile with correct shape
deleteFile	Mock unlink, verify called with correct path
1.7 apps/api/src/lib/cms.test.ts — 1 objek, ~81 baris source
Test	Approach
Mock globalThis.fetch, test tiap method (faq.list, articles.list, dll) return data dengan format benar	 
Test error handling saat CMS down (fetch throw → promise reject)	 
1.8 apps/api/src/middleware/auth.test.ts — 2 fungsi, ~35 baris source
Test	Approach
authMiddleware — valid Bearer token	Mock c.req.header('Authorization') return 'Bearer valid-token', mock verifyAccessToken return payload, verify c.set called
authMiddleware — no token	Mock header return undefined, verify unauthorized called
authMiddleware — invalid token	Mock verifyAccessToken throw, verify unauthorized called with "Invalid or expired"
authMiddleware — token from cookie	Mock header undefined, mock getCookie return token, verify success
requireRole — allowed role	Set c.get('userRole') return 'admin', call with roles=['admin'], verify next() called
requireRole — forbidden role	Set c.get('userRole') return 'customer', call with roles=['admin'], verify forbidden called
requireRole — multiple allowed roles	Set role 'dispatcher', verify allowed
1.9 apps/api/src/middleware/error-handler.test.ts — ~20 baris source
Test	Approach
errorHandler with HTTPException	Create HTTPException(404, 'Not found'), verify response JSON with status 404
errorHandler with generic Error	Mock console.error, verify response 500 via serverError
errorHandler with userId in context	Mock c.get('userId'), verify log includes userId
1.10 apps/api/src/middleware/rate-limiter.test.ts — ~88 baris source
Test	Approach
rateLimit() — first request	Verify next() called (not limited)
rateLimit() — exceed limit	Call 21× in same window, verify return 429
rateLimit() — window reset	Call 21×, wait mock timer, call again → 200 (not limited)
rateLimit() — different IP	21× from IP A limited, IP B not limited
rateLimit() — Redis path	Mock getRedis() return mock Redis, verify Redis rate limiting path
Fase 2 — Perbaiki Test Route yang Ada (P1)
Target: Response body + mock assertion + missing endpoints.
2.1 Tambah Response Body Assertion
Di semua file route test, ubah pattern:
// FROM (cuma cek status code)
const res = await app.request('/path');
expect(res.status).toBe(200);

// TO (cek body juga)
const res = await app.request('/path');
const body = await res.json() as ApiTestResponse<...>;
expect(res.status).toBe(200);
expect(body.success).toBe(true);
expect(body.data).toBeDefined();
2.2 Tambah Mock Verification
Di test yang trigger side effect (audit log, email, notifikasi), tambah:
expect(mockAudit.createAuditLog).toHaveBeenCalledWith(
  expect.anything(),
  expect.objectContaining({ action: 'booking.confirm' }),
);
2.3 Tambah Missing Endpoints
Route file	Endpoint ke-cover
bookings.test.ts	GET /:id, POST /:id/reject, POST /:id/on-the-way, POST /:id/start, POST /:id/complete
auth.test.ts	POST /convert-guest, POST /verify-email, POST /resend-verification
payments.test.ts	Verify dengan status Failed, duplicate verify (409)
2.4 Tambah Edge Case dari docs/testing/test-cases.md
Prioritas sesuai dokumen (P0 > P1 > P2):
- BOOK-006: Tanggal sebelum hari ini → validation error
- BOOK-010: Double submit → cuma 1 order
- BOOK-015: Nomor HP Indonesia valid
- AUTH-006: Email invalid format
- AUTH-008: Session expired → redirect login
Fase 3 — Database Schema Integrity (P2)
Target: 37 file schema terverifikasi.
packages/database/src/schema.test.ts
Test	Approach
Setiap tabel terekspor dari schema/index.ts	import * as schema from './schema', verify 32+ key exists
Setiap tabel punya id kolom UUID	Verify pgTable result has id with .primaryKey()
Relasi terekspor dari relations.ts	Verify relations() dipanggil untuk tabel2 yang punya relasi
Tabel soft-delete punya deletedAt	Scan kolom untuk deletedAt
Enum types match @specialist/types	Verify $type<UserRole>() dll
client.ts export db	Verify drizzle() dipanggil dengan correct config
Ini test tanpa koneksi DB — cukup import dan verify struktur objek.
Fase 4 — UI Component Tests (P2)
Target: 13 komponen di packages/ui/src/components/.
Infrastructure
- packages/ui/vitest.config.ts dengan environment: 'jsdom'
- packages/ui/src/test-setup.ts dengan @testing-library/jest-dom (auto cleanup)
- packages/ui/package.json: tambah "test": "vitest run", "test:watch": "vitest"
Komponen & Test Per Komponen
Komponen	Test Cases
Button	render default, render with label, disabled state, click handler, variant (primary/secondary/outline/danger), loading state
Input	render, placeholder, value, onChange, error state, disabled, label, type variants (text/password/email/number)
Select	render options, value selection, onChange, placeholder option, disabled
Textarea	render, value, onChange, rows, maxLength, placeholder, disabled
FileUpload	render, file select, accept types, max size, multiple files, drag-and-drop (jika ada)
DatePicker	render, date select, min/max date, disabled, format display
Pagination	render page numbers, prev/next button, active page, disabled prev on first page, disabled next on last page, onPageChange
Table	render headers, render rows, empty state, sortable header, loading skeleton
Modal	render closed, render open, close on overlay click, close on Escape, close button, prevent scroll, children render
Badge	render text, variant (success/warning/error/info), small/medium/large
Card	render title, render children, shadow variant, padding variant, with/without border
Container	render children, max-width variant (sm/md/lg/xl), padding variant
Typography	render h1-h6, paragraph, span, variant (heading/body/caption), color variant
Fase 5 — Web Component Tests (P3)
Target: 27 komponen React di apps/web/src/components/.
Infrastructure
- apps/web/vitest.config.ts dengan environment: 'jsdom' + setupFiles
- apps/web/src/test-setup.ts dengan @testing-library/jest-dom
- apps/web/package.json: tambah "test": "vitest run"
Prioritas Komponen (berdasarkan docs/testing/testing-strategy.md)
Prioritas	Komponen	Test
P0	LoginForm, RegisterForm, BookingForm, AuthGuard	Render, validation, submit flow, error state, role guard logic
P1	ForgotPasswordForm, ResetPasswordForm, CorporateInquiryForm, PartnerRegistrationForm, VerifyEmailForm	Form validation, API call mocks
P2	ServiceList, TrackingForm, CustomerOverview, PartnerOverview, AdminOverview	Render data, empty state, loading skeleton
P3	Semua komponen dashboard customer/partner/admin/corporate lainnya	Render, interaction, edge cases
Fase 6 — Perbaiki Test Validation & Shared (P3)
packages/validation/src/booking.test.ts
Tambah test untuk:
- acceptAssignmentSchema — 0 test saat ini
- mediaIdsSchema — UUID array valid, invalid UUID, >10 items
- Semua boundary value: min(1), max(255), min(3), max(500), dll
- phone regex (^\+?[0-9]+$)
- bookingDate regex pattern
- bookingTime regex pattern
packages/shared/src/helpers/date.test.ts
Tambah test untuk:
- isBookingPast — 0 test saat ini
- isWithinHours — 0 test saat ini
- parseBookingDateTime — invalid input
- getDateRange — verifikasi tanggal spesifik (bukan cuma regex)
Semua file validation test
Tambah assertion result.error?.issues untuk verifikasi field name dan error message spesifik.
Fase 7 — Playwright E2E (P3)
Infrastructure
1. Install Playwright: pnpm add -D @playwright/test di root
2. playwright.config.ts di root
3. Docker profile test di docker-compose.yml (bootstrap DB + API + Web)
4. Script seed data khusus test
5. GitHub Actions workflow untuk E2E
Flow yang Diimplementasi (prioritas P0 dari docs/testing/e2e-flows.md)
Flow ID	Flow	Pages to test
E2E-001	Guest Booking → Admin Dashboard → Booking muncul	/ → Booking → Submit → Admin Dashboard
E2E-002	Guest Tracking → Booking number → Timeline	/tracking → Input BN → Verify
E2E-003	Customer Login → Dashboard → History → Profile → Logout	Full auth flow
E2E-006	Partner Login → Dashboard → Assignment	Partner dashboard
E2E-014	Admin Login → Dashboard	Admin dashboard
E2E-015	Admin Assign Partner	Booking → Assign → Save
E2E-022	Search → Result → Service Detail	/ → Search → Click → Detail
E2E-026	Permission Test — Customer buka /admin → 403	Role guard
Cross-Module	Full lifecycle: Booking → Assign → Accept → Work → Complete → Payment → Verify → Review	End-to-end
Fase 8 — True Integration Tests (P4)
Target: Test endpoint dengan database real (PostgreSQL) — sesuai testing-strategy.md line 851.
Infrastructure
- testcontainers untuk PostgreSQL di CI
- Script db:test:seed dengan data fixture
- File apps/api/src/routes/*.integration.test.ts (pisah dari unit test)
- Jalankan hanya via pnpm test:integration (bukan default pnpm test)
Yang di-test dengan DB real
- Booking state machine (transitions, constraints)
- Unique constraints (email, phone, booking number)
- Transaction rollback (concurrent booking requests)
- Cascade delete behavior
- Rating/aggregate queries (ORDER BY, GROUP BY)
Ringkasan Timeline & Effort
Fase	Area	File Count	Test Count (est.)	Effort
F0	Infra & Fixes	~12 config	—	2 hari
F1	lib/ + middleware/ test	12 test files	~60 tests	5 hari
F2	Route test perbaikan	~15 files	~200 assertions added	4 hari
F3	Database schema test	1 test file	~20 tests	1 hari
F4	UI component test	13 test files	~80 tests	4 hari
F5	Web component test	~15 test files	~90 tests	5 hari
F6	Validation & shared perbaikan	~12 files	~60 tests	2 hari
F7	Playwright E2E	~12 spec files	~30 flows	7 hari
F8	True integration test	~8 test files	~40 tests	5 hari
Total	 	 	~580 tests	~35 hari
# Questions
Apakah Anda setuju dengan pendekatan 8 fase ini? Ada penyesuaian prioritas atau area yang ingin difokuskan lebih dulu?
Setuju, lanjutkan
Untuk Fase 7 (Playwright E2E), bagaimana preferensi environment testing?
Docker compose test profile
Untuk Fase 0.3 (perbaiki test API yang ada), apakah saya perlu detailkan setiap perubahan per file sekarang, atau langsung implement sesuai pola?
Langsung implement sesuai pola
+ Thought: 2.4s
Terima kasih konfirmasinya. Berikut rencana final yang sudah disesuaikan:
Rencana Final — Testing Spesialis Platform
Prioritas: Fase 0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8
Fase 0 — Foundation (2 hari)
Task	File yang dibuat/dimodifikasi
Setup Vitest di packages/database	packages/database/vitest.config.ts, package.json + "test" script, vitest devDep
Setup Vitest + Testing Library di packages/ui	packages/ui/vitest.config.ts (jsdom), package.json + "test" script, vitest + @testing-library/react + @testing-library/jest-dom + jsdom
Setup Vitest + Testing Library di apps/web	Sama seperti ui
Tambah coverage config di semua vitest.config	apps/api/, packages/shared/, packages/validation/, + yang baru
Perbaiki test API existing	Semua route test: tambah res.json() assertion + mock verification + hapus console.error
Update turbo.json	"test": {} tanpa dependsOn: ["^build"]
Fase 1 — lib/ + middleware/ Tests (5 hari)
12 test files baru — detail per-file sudah dijelaskan di rencana sebelumnya. Ringkasan:
File	Source baris	Fungsi	Test count
apps/api/src/lib/auth.test.ts	79	8 fungsi auth	~12
apps/api/src/lib/audit.test.ts	25	1 fungsi	~4
apps/api/src/lib/email.test.ts	265	7 fungsi email	~15
apps/api/src/lib/notification.test.ts	44	2 fungsi	~6
apps/api/src/lib/redis.test.ts	55	1 fungsi	~5
apps/api/src/lib/storage.test.ts	60	5 fungsi	~10
apps/api/src/lib/cms.test.ts	81	1 objek API client	~8
apps/api/src/middleware/auth.test.ts	35	2 middleware	~8
apps/api/src/middleware/error-handler.test.ts	20	1 handler	~4
apps/api/src/middleware/rate-limiter.test.ts	88	1 factory	~8
Fase 2 — Route Test Enhancement (4 hari)
Perbaiki ~15 file route test yang sudah ada:
- Tambah res.json() assertion di semua test
- Tambah mock verification (toHaveBeenCalled)
- Tambah missing endpoints: GET /:id, POST /:id/reject, POST /convert-guest, POST /verify-email, dll
- Tambah edge case dari docs/testing/test-cases.md (BOOK-006, BOOK-010, BOOK-015, AUTH-006, AUTH-008)
Fase 3 — Database Schema Test (1 hari)
1 file: packages/database/src/schema.test.ts
- Verifikasi 32 tabel + relations terekspor dengan benar
- Verifikasi primary key, soft delete, enum types
Fase 4 — UI Component Tests (4 hari)
13 test files di packages/ui/src/components/*.test.tsx
- Button, Input, Select, Textarea, FileUpload, DatePicker, Pagination
- Table, Modal, Badge, Card, Container, Typography
- Render, interaction, accessibility, variants, states
Fase 5 — Web Component Tests (5 hari)
~15 test files di apps/web/src/components/*.test.tsx
- Prioritas P0: LoginForm, RegisterForm, BookingForm, AuthGuard
- Prioritas P1: ForgotPasswordForm, ResetPasswordForm, CorporateInquiryForm, dll
- Prioritas P2: ServiceList, TrackingForm, dashboard overviews
Fase 6 — Validation & Shared Enhancement (2 hari)
- validation/src/booking.test.ts: tambah acceptAssignmentSchema, mediaIdsSchema, boundary value untuk semua field
- shared/src/helpers/date.test.ts: tambah isBookingPast, isWithinHours, specific date verification
- Semua validation test: tambah assertion result.error?.issues untuk field name spesifik
Fase 7 — Playwright E2E (7 hari)
Environment: Docker compose test profile (docker compose --profile test up)
- Seed data khusus test
- API + Web + PostgreSQL running
12 spec files mencakup 30 flow:
Prioritas	Flow
P0	Guest Booking + Admin Dashboard verify (E2E-001)
P0	Guest Tracking (E2E-002)
P0	Customer Login → Dashboard → History → Profile → Logout (E2E-003)
P0	Partner Login → Dashboard → Assignment (E2E-006)
P0	Admin Login → Dashboard (E2E-014)
P0	Admin Assign Partner (E2E-015)
P0	Cross-module lifecycle (Booking → Assign → Accept → Work → Complete → Payment → Review)
P1	Customer Repeat Booking (E2E-004), Customer Review (E2E-005)
P1	Partner Accept/Reject/Complete (E2E-007, E2E-008, E2E-009)
P1	Permission test (E2E-026), Forgot Password (E2E-025)
P1	Responsive test (E2E-028), Accessibility test (E2E-029)
P2	Search (E2E-022), Corporate flows (E2E-010–013)
Fase 8 — True Integration Tests (5 hari)
~8 file *.integration.test.ts dengan PostgreSQL real via Testcontainers
- Booking state machine transitions
- Unique constraint violations (email, phone, booking number)
- Transaction rollback / concurrent requests
- Cascade delete, aggregate queries
Ringkasan Total: ~580 test, ~35 hari
Fase	Test count	File count	Effort
F0 - Foundation	—	~12	2 hari
F1 - lib & middleware	~80	12	5 hari
F2 - Route enhancement	~200	15	4 hari
F3 - Database schema	~20	1	1 hari
F4 - UI components	~80	13	4 hari
F5 - Web components	~90	15	5 hari
F6 - Validation & shared	~60	12	2 hari
F7 - Playwright E2E	~30	12	7 hari
F8 - True integration	~40	8	5 hari
Total	~580	~100	~35 hari
