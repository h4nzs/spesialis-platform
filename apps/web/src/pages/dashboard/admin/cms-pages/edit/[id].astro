---
export const prerender = false
import DashboardLayout from '../../../../../layouts/DashboardLayout.astro'
import { PageEditor } from '../../../../../components/dashboard/admin/PageEditor'

const { id } = Astro.params
---

<DashboardLayout title="Edit Halaman" role={['admin', 'super_admin', 'content_manager']}>
  <PageEditor client:load editingId={id} />
</DashboardLayout>
