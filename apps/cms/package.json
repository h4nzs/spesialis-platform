{
  "name": "@specialist/cms",
  "private": true,
  "type": "module",
  "description": "Directus CMS configuration & extensions",
  "scripts": {
    "build:extensions": "cd src/extensions/seo-revalidation && npx directus-extension build && cd ../dashboard-stats && npx directus-extension build"
  }
}
